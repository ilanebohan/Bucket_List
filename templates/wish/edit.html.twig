{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Add{% endblock %}

{% block body %}
    {% if id is defined %}
        <h2>Editer ce voeux</h2>
    {% else %}
        <h2>Créer un nouveau voeux</h2>
    {% endif %}

    {{ form_start(form) }}

    {{ form_label(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}

    {{ form_end(form) }}
{% endblock %}